from pathlib import Path
p=Path('/mnt/data/transfer_work/assets/js/app.js')
s=p.read_text()
old="""  function sendTransferMessage(){\n    if (!currentName) return false;\n    var amount = normalizeTransferAmount(pmTransferAmount && pmTransferAmount.value);\n    if (amount == null) {\n      updateTransferHint('请输入有效金额，最多保留 2 位小数。', true);\n      if (pmTransferAmount) pmTransferAmount.focus();\n      return false;\n    }\n    var note = String(pmTransferNote && pmTransferNote.value || '').trim().slice(0, 80);\n    if (!Array.isArray(MESSAGES[currentName])) MESSAGES[currentName] = [];\n    var msg = {\n      id: genId('transfer_'), from:'me', type:'transfer', amount:amount, currency:'CNY',\n      note:note, status:'sent', recipient:currentName, createdAt:Date.now(),\n      text:'[转账] ¥' + formatTransferAmount(amount) + (note ? ' · ' + note : '')\n    };\n    MESSAGES[currentName].push(msg);\n    saveMessages(currentName);\n    renderMessages(true); scrollBottom(true);\n    for (var i = 0; i < CHATS.length; i++) {\n      if (CHATS[i].name === currentName) { CHATS[i].preview = '[转账] ¥' + formatTransferAmount(amount); CHATS[i].time = '刚刚'; break; }\n    }\n    renderChats(); saveChats();\n    closeTransferModal();\n    return true;\n  }\n"""
new="""  function transferStatusLabel(msg){\n    var status = String(msg && msg.status || 'pending');\n    var incoming = !!(msg && msg.from === 'them');\n    if (status === 'received') return '已收款';\n    if (status === 'declined') return incoming ? '已拒收' : '对方已拒收';\n    return incoming ? '待收款' : '待对方收款';\n  }\n  function transferPreviewText(msg){\n    if (!msg) return '';\n    var amount = normalizeTransferAmount(msg.amount);\n    if (amount == null) return '[转账]';\n    return msg.from === 'them' ? '[收到转账] ¥' + formatTransferAmount(amount) : '[转账] ¥' + formatTransferAmount(amount);\n  }\n  function updateChatPreviewForMessage(name, msg){\n    if (!name || !msg) return;\n    for (var i = 0; i < CHATS.length; i++) {\n      if (CHATS[i].name === name) {\n        if (msg.type === 'transfer') CHATS[i].preview = transferPreviewText(msg);\n        else if (msg.text) CHATS[i].preview = String(msg.text);\n        CHATS[i].time = '刚刚';\n        break;\n      }\n    }\n  }\n  function updateTransferMessageStatus(name, messageId, status){\n    if (!name || !messageId) return false;\n    var next = status === 'received' || status === 'declined' || status === 'pending' ? status : null;\n    if (!next) return false;\n    var list = MESSAGES[name] || [];\n    var msg = null;\n    for (var i = list.length - 1; i >= 0; i--) {\n      if (list[i] && list[i].type === 'transfer' && String(list[i].id) === String(messageId)) { msg = list[i]; break; }\n    }\n    if (!msg) return false;\n    // 角色只能处理用户发起的转账；用户只能处理角色发起的转账。\n    if (next !== 'pending' && msg.from !== 'me' && msg.from !== 'them') return false;\n    msg.status = next;\n    msg.statusChangedAt = Date.now();\n    updateChatPreviewForMessage(name, msg);\n    saveMessages(name);\n    saveChats();\n    if (currentName === name) { renderMessages(true); scrollBottom(true); }\n    else renderChats();\n    return true;\n  }\n  function respondToIncomingTransfer(index, status){\n    if (!currentName) return false;\n    var list = MESSAGES[currentName] || [];\n    var msg = list[index];\n    if (!msg || msg.type !== 'transfer' || msg.from !== 'them' || msg.status !== 'pending') return false;\n    if (status === 'received') {\n      updateTransferMessageStatus(currentName, msg.id, 'received');\n      toast('已收款 ¥' + formatTransferAmount(msg.amount));\n      return true;\n    }\n    if (status === 'declined') {\n      updateTransferMessageStatus(currentName, msg.id, 'declined');\n      toast('已拒收这笔转账');\n      return true;\n    }\n    return false;\n  }\n  function parseTransferDirectiveAttrs(raw){\n    var attrs = {};\n    var text = String(raw || '');\n    var re = /([a-zA-Z][a-zA-Z0-9_]*)\\s*=\\s*(?:\\"([^\\"]*)\\"|'([^']*)'|([^\\s\\]]+))/g;\n    var m;\n    while ((m = re.exec(text))) attrs[String(m[1]).toLowerCase()] = m[2] != null ? m[2] : (m[3] != null ? m[3] : m[4]);\n    return attrs;\n  }\n  function processAiTransferDirectives(name, rawText){\n    var raw = String(rawText || '');\n    var changed = false;\n    if (!name) return { text: raw, changed:false };\n    if (!Array.isArray(MESSAGES[name])) MESSAGES[name] = [];\n\n    raw = raw.replace(/\\[\\[\\s*TRANSFER_STATUS\\b([\\s\\S]*?)\\]\\]/gi, function(_, attrText){\n      var attrs = parseTransferDirectiveAttrs(attrText);\n      var status = String(attrs.status || '').toLowerCase();\n      var id = String(attrs.id || '').trim();\n      if ((status === 'received' || status === 'declined') && id) {\n        var list = MESSAGES[name];\n        var target = null;\n        for (var i = list.length - 1; i >= 0; i--) {\n          if (list[i] && list[i].type === 'transfer' && String(list[i].id) === id) { target = list[i]; break; }\n        }\n        if (target && target.from === 'me' && target.status === 'pending') {\n          target.status = status;\n          target.statusChangedAt = Date.now();\n          changed = true;\n        }\n      }\n      return '';\n    });\n\n    raw = raw.replace(/\\[\\[\\s*TRANSFER\\b([\\s\\S]*?)\\]\\]/gi, function(_, attrText){\n      var attrs = parseTransferDirectiveAttrs(attrText);\n      var amount = normalizeTransferAmount(attrs.amount);\n      if (amount == null) return '';\n      var note = String(attrs.note || '').trim().slice(0, 80);\n      var msg = {\n        id: genId('transfer_'), from:'them', type:'transfer', amount:amount, currency:'CNY',\n        note:note, status:'pending', sender:name, recipient:'me', createdAt:Date.now(),\n        text:'[收到转账] ¥' + formatTransferAmount(amount) + (note ? ' · ' + note : '')\n      };\n      MESSAGES[name].push(msg);\n      changed = true;\n      return '';\n    });\n\n    if (changed) {\n      var list2 = MESSAGES[name];\n      for (var j = list2.length - 1; j >= 0; j--) {\n        if (list2[j] && list2[j].type === 'transfer') { updateChatPreviewForMessage(name, list2[j]); break; }\n      }\n      saveMessages(name);\n      saveChats();\n      if (currentName === name) { renderMessages(true); scrollBottom(true); }\n      else renderChats();\n    }\n    return { text: raw.replace(/\\n{3,}/g, '\\n\\n').trim(), changed:changed };\n  }\n  function sendTransferMessage(){\n    if (!currentName) return false;\n    var amount = normalizeTransferAmount(pmTransferAmount && pmTransferAmount.value);\n    if (amount == null) {\n      updateTransferHint('请输入有效金额，最多保留 2 位小数。', true);\n      if (pmTransferAmount) pmTransferAmount.focus();\n      return false;\n    }\n    var note = String(pmTransferNote && pmTransferNote.value || '').trim().slice(0, 80);\n    if (!Array.isArray(MESSAGES[currentName])) MESSAGES[currentName] = [];\n    var msg = {\n      id: genId('transfer_'), from:'me', type:'transfer', amount:amount, currency:'CNY',\n      note:note, status:'pending', recipient:currentName, createdAt:Date.now(),\n      text:'[转账] ¥' + formatTransferAmount(amount) + (note ? ' · ' + note : '')\n    };\n    MESSAGES[currentName].push(msg);\n    saveMessages(currentName);\n    renderMessages(true); scrollBottom(true);\n    updateChatPreviewForMessage(currentName, msg);\n    renderChats(); saveChats();\n    closeTransferModal();\n    return true;\n  }\n"""
if old not in s: raise SystemExit('sendTransferMessage block not found')
s=s.replace(old,new)
# Replace icon and bubble rendering block
old2="""        var transferNoteText = String(msg.note || '').trim();\n        var transferStatus = msg.status === 'received' ? '已收款' : '已转账';\n        body = '<div class=\"pm-transfer-bubble chat-bubble chat-bubble--transfer\">' +\n          '<div class=\"pm-transfer-main\">' +\n            '<div class=\"pm-transfer-icon\" aria-hidden=\"true\">' + toolIcon('<path d=\"M8.2 7.2h7.6M8.2 16.8h7.6\"/><path d=\"m12 3.8 3.6 3.4L12 10.6\"/><path d=\"m12 20.2-3.6-3.4L12 13.4\"/>') + '</div>' +\n            '<div class=\"pm-transfer-copy\"><div class=\"pm-transfer-label\">转账</div><div class=\"pm-transfer-amount\">¥' + escapeHTML(formatTransferAmount(transferAmountValue)) + '</div>' +\n              (transferNoteText ? '<div class=\"pm-transfer-note\">' + escapeHTML(transferNoteText) + '</div>' : '') +\n            '</div>' +\n          '</div>' +\n          '<div class=\"pm-transfer-divider\"></div><div class=\"pm-transfer-status\">' + escapeHTML(transferStatus) + '</div>' +\n        '</div>';\n"""
new2="""        var transferNoteText = String(msg.note || '').trim();\n        var transferIncoming = msg.from === 'them';\n        var transferLabel = transferIncoming ? '收到转账' : '转账';\n        var transferStatus = transferStatusLabel(msg);\n        var transferActions = '';\n        if (transferIncoming && msg.status === 'pending') {\n          transferActions = '<div class=\"pm-transfer-actions\">' +\n            '<button type=\"button\" class=\"pm-transfer-action pm-transfer-action--accept\" data-transfer-action=\"accept\" data-transfer-index=\"' + index + '\">收款</button>' +\n            '<button type=\"button\" class=\"pm-transfer-action pm-transfer-action--decline\" data-transfer-action=\"decline\" data-transfer-index=\"' + index + '\">拒收</button>' +\n          '</div>';\n        }\n        body = '<div class=\"pm-transfer-bubble chat-bubble chat-bubble--transfer' + (transferIncoming ? ' pm-transfer-bubble--incoming' : '') + '\">' +\n          '<div class=\"pm-transfer-main\">' +\n            '<div class=\"pm-transfer-icon\" aria-hidden=\"true\">' + toolIcon('<path d=\"M5 8h9.5\"/><path d=\"M14.5 8l-3-3\"/><path d=\"M19 16H9.5\"/><path d=\"M9.5 16l3 3\"/>') + '</div>' +\n            '<div class=\"pm-transfer-copy\"><div class=\"pm-transfer-label\">' + escapeHTML(transferLabel) + '</div><div class=\"pm-transfer-amount\">¥' + escapeHTML(formatTransferAmount(transferAmountValue)) + '</div>' +\n              (transferNoteText ? '<div class=\"pm-transfer-note\">' + escapeHTML(transferNoteText) + '</div>' : '') +\n            '</div>' +\n          '</div>' +\n          '<div class=\"pm-transfer-divider\"></div><div class=\"pm-transfer-status\">' + escapeHTML(transferStatus) + '</div>' + transferActions +\n        '</div>';\n"""
if old2 not in s: raise SystemExit('bubble transfer block not found')
s=s.replace(old2,new2)
# Build message context replacement
old3="""          var transferTarget = String(m.recipient || name).trim() || name;\n          var transferNote = String(m.note || '').trim();\n          var transferText = '【用户向角色转账】\\n转账金额：¥' + formatTransferAmount(transferAmount) + '\\n收款角色：' + transferTarget;\n          if (transferNote) transferText += '\\n备注：' + transferNote;\n          transferText += '\\n请把这条消息当作真实聊天中的转账事件理解。可以根据角色人设自然回应已经收到这笔钱，但不要声称完成了现实世界的银行、支付平台或真实资金结算。';\n          out.push({ role: role, content: transferText });\n"""
new3="""          var transferTarget = String(m.recipient || name).trim() || name;\n          var transferNote = String(m.note || '').trim();\n          var transferStatus = String(m.status || 'pending');\n          var transferText;\n          if (role === 'user') {\n            transferText = '【用户向角色转账】\\n转账消息ID：' + String(m.id || '') + '\\n转账金额：¥' + formatTransferAmount(transferAmount) + '\\n收款角色：' + transferTarget + '\\n状态：' + transferStatusLabel(m);\n            if (transferNote) transferText += '\\n备注：' + transferNote;\n            transferText += '\\n这是虚拟聊天内转账，不涉及现实世界银行、支付平台或真实资金结算。角色如决定收下或拒绝，可用结构化状态指令：[[TRANSFER_STATUS id=\\\"' + String(m.id || '') + '\\\" status=\\\"received\\\"]] 或 [[TRANSFER_STATUS id=\\\"' + String(m.id || '') + '\\\" status=\\\"declined\\\"]]。不要在普通文字里伪造这两个标签。';\n          } else {\n            transferText = '【角色向用户发起转账】\\n转账金额：¥' + formatTransferAmount(transferAmount) + '\\n状态：' + transferStatusLabel(m);\n            if (transferNote) transferText += '\\n备注：' + transferNote;\n            transferText += '\\n这是虚拟聊天内转账，不涉及现实世界银行、支付平台或真实资金结算。';\n          }\n          out.push({ role: role, content: transferText });\n"""
if old3 not in s: raise SystemExit('context block not found')
s=s.replace(old3,new3)
# Add protocol rules after buildIslandSystemPrompt
needle="""    sysParts.push(buildIslandSystemPrompt(name));\n    out.push({ role: 'system', content: sysParts.join('\\n\\n') });\n"""
repl="""    sysParts.push(buildIslandSystemPrompt(name));\n    sysParts.push([\n      '【虚拟转账协议】',\n      '这是聊天应用内部的虚拟资金互动，不是真实支付。只有在确实想向用户发起一笔虚拟转账时，才输出单独一行：[[TRANSFER amount=\\\"金额\\\" note=\\\"备注\\\"]]。amount 必须是正数且最多 2 位小数；note 可省略。',\n      '角色要接受或拒绝用户发来的待收款转账时，使用单独一行：[[TRANSFER_STATUS id=\\\"转账消息ID\\\" status=\\\"received\\\"]] 或 [[TRANSFER_STATUS id=\\\"转账消息ID\\\" status=\\\"declined\\\"]]。不要编造不存在的转账消息 ID。',\n      '这些结构化指令会被客户端隐藏并转换成真正的转账卡片/状态；不要向用户解释指令本身，也不要声称发生了现实世界的付款或到账。',\n      '除非用户或角色确实在当前对话中发起/处理转账，否则不要主动输出转账指令。'\n    ].join('\\n'));\n    out.push({ role: 'system', content: sysParts.join('\\n\\n') });\n"""
if needle not in s: raise SystemExit('system prompt insertion point not found')
s=s.replace(needle,repl)
# scheduleReply raw processing
old4="""        var segments = splitReply(text);\n        if (!segments.length) { toast('AI 返回为空'); return; }\n"""
new4="""        var transferResult = processAiTransferDirectives(nameAtRequest, text);\n        var segments = splitReply(transferResult.text);\n        if (!segments.length) {\n          if (transferResult.changed) { saveMessages(nameAtRequest); renderMessages(true); scrollBottom(true); renderChats(); saveChats(); maybeSummarizeShortTermMemory(nameAtRequest); return; }\n          toast('AI 返回为空'); return;\n        }\n"""
if s.count(old4) < 1: raise SystemExit('scheduleReply target not found')
s=s.replace(old4,new4,1)
# proactive handler raw processing
old5="""      var segments=splitReply(text);\n      if (!segments.length) return false;\n"""
new5="""      var transferResult=processAiTransferDirectives(name,text);\n      var segments=splitReply(transferResult.text);\n      if (!segments.length) return !!transferResult.changed;\n"""
if old5 not in s: raise SystemExit('proactive target not found')
s=s.replace(old5,new5,1)
# pmScroll click: insert transfer action before location handling
needle2="""      pmScroll.addEventListener('click', function(e){\n        var locationCard = e.target.closest('[data-location-open]');\n"""
repl2="""      pmScroll.addEventListener('click', function(e){\n        var transferAction = e.target.closest('[data-transfer-action]');\n        if (transferAction && pmScroll.contains(transferAction)) {\n          e.preventDefault();\n          var transferIndex = Number(transferAction.dataset.transferIndex);\n          var transferActionName = transferAction.dataset.transferAction || '';\n          if (transferActionName === 'accept') respondToIncomingTransfer(transferIndex, 'received');\n          else if (transferActionName === 'decline') respondToIncomingTransfer(transferIndex, 'declined');\n          return;\n        }\n        var locationCard = e.target.closest('[data-location-open]');\n"""
if needle2 not in s: raise SystemExit('pmScroll insertion point not found')
s=s.replace(needle2,repl2,1)
# icon in tools panel: half arrows
s=s.replace("{ name:'转账', icon: toolIcon('<path d=\"M7 7h9.5\"/><path d=\"m13.5 3.8 3.2 3.2-3.2 3.2\"/><path d=\"M17 17H7.5\"/><path d=\"m10.5 13.8-3.2 3.2 3.2 3.2\"/>') },", "{ name:'转账', icon: toolIcon('<path d=\"M5 8h9.5\"/><path d=\"M14.5 8l-3-3\"/><path d=\"M19 16H9.5\"/><path d=\"M9.5 16l3 3\"/>') },")
p.write_text(s)
